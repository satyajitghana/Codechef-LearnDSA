#include <bits/stdc++.h>

using namespace std;

#define lld long long int

int main() {
    int T;
    cin >> T;
    while (T--) {
        lld K;
        lld d0, d1;
        cin >> K >> d0 >> d1;

        lld num = (d0 + d1);

        for (int i = 0; i < K; i++) {
            num
        }
    }

    return 0;
}